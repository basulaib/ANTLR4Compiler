//test for conjunction/equivalence
class test{
	declare {
    	bool result;
    }

	func bool imp(bool x, bool y){
		require {
            x == true || false;
            y == true || false;
		}

		result = x => y;

		ensure {
			result == (x => y);
		}
	}

	assert {
		result == true || false;
	}
}