class Account{
	declare {
    	int	amount;
		int a = 5;
    }

	assume{
		amount > 0;	
	}
    
//   amount = 5;
//   ID = 8;
	
	func int increase(int x, int y){
		require {
			x > 0;
		}
		amount = amount + x;
        
        if (x > 10) {
        	 x = p || q && r;
             x = p && q || r;
        }else if (x > 9) {
        
        }else {
        
        } 
        
		ensure {
			amount == amount + x;
		}		
	}
	
	assert {
		amount > 0;
	}
}