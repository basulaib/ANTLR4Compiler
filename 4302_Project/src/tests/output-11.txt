
open util/boolean

//==============test==============
some sig test{
	result:Bool
}

fact testFact {
}

assert testAssert {
	all n: test| ((n.result) = (True)) or (False)
}

//the class invariant for class (test) is only valid if this check is valid
check testAssert

pred smallEqualCheck[x:Int, y:Int]{
	some n: test| not ((((x) > (0)) and ((y) > (0))) => ((((x) <= (y)) => ((True) = ((x) <= (y)))) and ((not ((x) <= (y))) => ((False) = ((x) <= (y))))))
}

//the post condition for function (smallEqual) is only valid when this is inconsistent.
run {
	some x: Int| some y: Int| smallEqualCheck[x, y]
}for 8 but 8 int, 2 Bool, exactly 32 String